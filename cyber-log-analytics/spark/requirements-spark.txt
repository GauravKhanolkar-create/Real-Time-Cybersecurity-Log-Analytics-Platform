# Extra Python packages needed *inside the Spark containers* to run
# spark_jobs/stream_processor.py. The official apache/spark image ships
# Python + PySpark, but not these.
#
# Versions match requirements.txt / the model shipped in ml/models/, to
# avoid any pickle/version mismatch when loading the trained
# IsolationForest + StandardScaler via joblib.
pyyaml==6.0.1
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1
joblib==1.4.2
